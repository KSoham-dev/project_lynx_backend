"""
agents/layer1/__init__.py
==========================
Layer 1 — Image Analysis Agent package.

Public entry point:
    from agents.layer1.image_agent import get_image_agent, set_speciesnet_model
    result: ImageAnalysisResult = await get_image_agent().run(image_url, lat, lon)
"""
