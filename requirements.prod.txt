# Minimal production requirements for the Prahari SpeciesNet API.
# Generated from requirements.txt – keeps only what main.py actually needs.
# speciesnet pulls torch/torchvision/numpy/pillow as its own deps,
# so pinning them here just ensures the versions stay consistent.

fastapi==0.134.0
uvicorn==0.41.0
pydantic==2.12.5
pydantic_core==2.41.5

# Azure Blob – IUCN species files (uses Managed Identity on ACA)
azure-storage-blob==12.28.0
azure-identity==1.25.2

# Species-traits pipeline
groq>=0.13.0
cachetools>=5.3.0
requests==2.32.5

speciesnet==5.0.3

# speciesnet hard-deps (pin to avoid re-solving)
torch==2.10.0
torchvision==0.25.0
numpy==2.4.2
pillow==12.1.1
pillow-avif-plugin==1.5.5
pi_heif==1.3.0
opencv-python-headless==4.10.0.84
requests==2.32.5
scipy==1.17.1
shapely==2.1.2
reverse_geocoder==1.5.1
